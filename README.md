logstash-practice
=================

```sh
# Manually open terminal and do this
cd /opt/boxen/repo && /opt/boxen/repo/script/boxen --no-fde

# Manually open terminal and do this
/opt/boxen/homebrew/Cellar/elasticsearch/1.3.4/bin/elasticsearch start

# Manually open terminal and do this
make test

# Manually open terminal and do this
curl 'http://localhost:9200/_search?pretty'
```
